--- a/docker-compose-coolify.yaml
+++ b/docker-compose-coolify.yaml
@@ -1,4 +1,4 @@
-version: "3.9"            # <-- you can remove this line, it's ignored by v2+ compose
+services:
 
 services:
   app:
@@ -6,8 +5,11 @@ services:
     image: bielnog600/bolt.feldev:main-ews840gskwg880ksck0w4ksw
     build:
       context: .
-      dockerfile: Dockerfile-coolify
+      dockerfile: Dockerfile-coolify
+      args:
+        PNPM_VERSION: ${PNPM_VERSION:-7.31.0}
 
     ports:
       - "3000:5173"
